describe('Fluxy Actions', function () {

  describe('service actions', function () {
    it('registers an action handler against the provided Constant');
    it('hooks up the handler for Constant_COMPLETED');
    it('hooks up the handler for Constant_FAILED');
  });

  describe('#dispatchAction', function () {
    it('dispatches the action through the Fluxy dispatcher');
  });

});
