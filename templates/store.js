var Fluxy = require('fluxy');

var PLACEHOLDER_NAME = Fluxy.createStore({
  getInitialState: function () {
    return {
    };
  },

  actions: [
    /*
    [Constants.ActionName, ActionHandler],
    */
  ]
});

module.exports = PLACEHOLDER_NAME;
