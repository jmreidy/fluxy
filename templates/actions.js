var Fluxy = require('fluxy');

var PLACEHOLDER_NAME = Fluxy.createActions({
  serviceActions: {
  /*
   * methodName: [Constant.VALUE, handler],
   */
  },
  //other actions  below
});

module.exports = PLACEHOLDER_NAME;
